{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing for the Taigi tone-sandhi logic in pinyin-preference-utils.ts. Target only the sandhi-related range (lines 312-431); the rest of the file is legacy code with prior coverage. Run with `bun run stryker`.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.unit.config.ts"
  },
  "mutate": ["src/utils/pinyin-preference-utils.ts:312-431"],
  "reporters": ["progress", "clear-text", "html", "json"],
  "coverageAnalysis": "perTest",
  "concurrency": 4,
  "timeoutMS": 15000,
  "thresholds": {
    "high": 100,
    "low": 100,
    "break": 100
  }
}
